{% block extends %}
{{ "{% extends '::layout.html.twig' %}" }}
{% endblock extends %}

{% block body %}
{{ "{% block page %}" }}
    <div class="row">
        <div class="small-12 large-12 columns">
            <h1 class="text-center title">{{ entity }} edit</h1>
        </div>
    </div>
    <div class="row">
        <div class="small-12 large-12 columns">
            {{ '{{ form(edit_form) }}' }}

            {% set hide_edit, hide_delete = true, false %}
            {% include 'crud/views/others/record_actions.html.twig.twig' %}
        </div>
    </div>
{{ "{% endblock %}" }}
{% endblock body %}
